@page "/reset-password"
@inject LoreLoomApiClient Api
@inject NavigationManager Nav
@inject ISnackbar Snackbar
@inject IAppTextLocalizer T

<div class="hero-section">
    <MudCard Class="auth-card" Style="width: 420px; max-width: 95vw;">
        <MudCardContent Class="pa-6">
            <div class="text-center mb-6">
                <MudText Typo="Typo.h4" Color="Color.Primary" Style="letter-spacing:0.04em;">LoreLoom</MudText>
                <MudText Typo="Typo.body2" Style="color:#9A97B0;" Class="mt-1">@T["reset_title"]</MudText>
            </div>

            @if (_invalidLink)
            {
                <MudAlert Severity="Severity.Error" Class="mb-4">
                    @T["reset_invalid_link"]
                </MudAlert>
                <MudButton Variant="Variant.Outlined" Color="Color.Primary" FullWidth="true"
                           Href="/forgot-password" Style="border-radius:12px;">
                    @T["reset_request_new_link"]
                </MudButton>
            }
            else if (_success)
            {
                <MudAlert Severity="Severity.Success" Class="mb-4">
                    @T["reset_success"]
                </MudAlert>
                <MudButton Variant="Variant.Filled" Color="Color.Primary" FullWidth="true"
                           Href="/login" Style="border-radius:12px;">
                    @T["common_go_to_login"]
                </MudButton>
            }
            else
            {
                <MudTextField @bind-Value="_newPassword" Label="@(T["reset_new_password"])" Variant="Variant.Outlined"
                              InputType="InputType.Password" Class="mb-4" Required="true"
                              HelperText="@(T["reset_password_helper"])"
                              Adornment="Adornment.Start" AdornmentIcon="@Icons.Material.Filled.Lock" />
                <MudTextField @bind-Value="_confirmPassword" Label="@(T["reset_confirm_password"])" Variant="Variant.Outlined"
                              InputType="InputType.Password" Class="mb-4" Required="true"
                              Adornment="Adornment.Start" AdornmentIcon="@Icons.Material.Filled.Lock"
                              OnKeyDown="OnKeyDown" />
            }
        </MudCardContent>

        @if (!_invalidLink && !_success)
        {
            <MudCardActions Class="pa-6 pt-0">
                <MudButton Variant="Variant.Filled" Color="Color.Primary" FullWidth="true"
                           OnClick="ResetAsync" Disabled="_loading"
                           Style="border-radius:12px;height:48px;font-weight:600;">
                    @if (_loading) { <MudProgressCircular Size="Size.Small" Indeterminate="true" Class="mr-2" /> }
                    @T["reset_button"]
                </MudButton>
            </MudCardActions>
        }
    </MudCard>
</div>

@code {
    private string _token = "";
    private string _newPassword = "";
    private string _confirmPassword = "";
    private bool _loading;
    private bool _success;
    private bool _invalidLink;

    protected override void OnInitialized()
    {
        var uri = new Uri(Nav.Uri);
        var query = System.Web.HttpUtility.ParseQueryString(uri.Query);
        _token = query["token"] ?? "";

        if (string.IsNullOrWhiteSpace(_token))
            _invalidLink = true;
    }

    private async Task OnKeyDown(KeyboardEventArgs e)
    {
        if (e.Key == "Enter") await ResetAsync();
    }

    private async Task ResetAsync()
    {
        if (string.IsNullOrWhiteSpace(_newPassword))
        {
            Snackbar.Add(T["reset_enter_password"], Severity.Warning);
            return;
        }

        if (_newPassword != _confirmPassword)
        {
            Snackbar.Add(T["register_passwords_no_match"], Severity.Warning);
            return;
        }

        if (_newPassword.Length < 8)
        {
            Snackbar.Add(T["register_password_min"], Severity.Warning);
            return;
        }

        _loading = true;
        try
        {
            await Api.ResetPasswordAsync(new ResetPasswordRequest(_token, _newPassword, _confirmPassword));
            _success = true;
        }
        catch (HttpRequestException ex)
        {
            if (ex.StatusCode == System.Net.HttpStatusCode.BadRequest)
                _invalidLink = true;
            else
                Snackbar.Add(T.Format("common_error", ex.Message), Severity.Error);
        }
        finally
        {
            _loading = false;
        }
    }
}
